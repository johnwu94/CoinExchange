version: 0.2

phases: 
  build:
    commands:
      #- mkdir alljar
      - ls
      - cd 00_framework
      - maven install
      - ./cp-jar.sh

  post_build:
    commands:
      - pwd
      - ls
    

artifacts: 
  files: 
    - "**/*.jar"
  discard-paths: yes
  base-directory: "alljar/"      